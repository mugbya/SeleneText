
```
[用户点击菜单栏“保存”按钮]
                │
                ▼
🟦 主进程：菜单点击回调
click: () => {
   win.webContents.send('menu-save');
}
                │
                ▼
🟩 渲染进程收到 menu-save
window.electronAPI.on('menu-save', () => {
   // 判断当前激活 tab 是不是新建文件
   // 是：调用 ipcRenderer.invoke('save-file-as', { content })
   // 否：调用 ipcRenderer.invoke('save-file', { path, content })
})
                │
                ▼
🟦 主进程收到 save-file / save-file-as
ipcMain.handle('save-file-as', async (event, { content }) => {
   // 弹出 dialog.showSaveDialog
   // 写入内容到文件系统
   // 通知 UI 保存成功
})

```
